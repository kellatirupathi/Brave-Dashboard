Here's the final, clean prompt to paste into the Replit agent:

Switch transactional email from Brevo to Amazon SES.

The app sends all transactional email through one function — sendEmail() in artifacts/api-server/src/lib/email/brevo.ts. Replace the Brevo implementation with Amazon SES, keeping the exact same exports and signatures so no other file needs to change.

1. Add the dependency

Add @aws-sdk/client-sesv2 to artifacts/api-server/package.json and install it.
2. Rewrite artifacts/api-server/src/lib/email/brevo.ts

Keep the same file path and the same exports — EmailRecipient, SendEmailInput, sendEmail(input): Promise<boolean>, and getAppUrl(). Do not rename anything (callers import from ./lib/email/brevo).
Inside sendEmail, send via SES using SESv2Client and SendEmailCommand from @aws-sdk/client-sesv2.
Read config from environment variables:
AWS_REGION (default ap-south-1)
EMAIL_FROM (default brave.niat@nxtwave.in)
The SDK auto-reads AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY from env — do not hard-code any credentials.
From address must be: BRAVE Dashboard <EMAIL_FROM>.
Build the SES command: FromEmailAddress, Destination.ToAddresses = the recipient email(s), Content.Simple.Subject.Data = subject, Content.Simple.Body.Text.Data = text.
Keep the existing safety behaviour exactly:
Accept a single recipient or an array; filter out entries whose email doesn't contain @.
If required AWS env vars are missing, or there are no valid recipients → log a warning and return false.
Catch every error, log it, return false. sendEmail must never throw.
On success, log info and return true.
Keep getAppUrl() exactly as it is.
Update the file's comments/header to say Amazon SES instead of Brevo.
3. Do NOT modify financials.ts, cron-reminders.ts, or cron.ts — they keep calling sendEmail() unchanged.

4. After the change, run a typecheck to confirm the api-server still compiles.

After you paste that — do this in Replit yourself
In Replit → Secrets (Tools → Secrets), add these 4 secrets (the agent can't set secret values, only you can):

Secret name	Value
AWS_REGION	ap-south-1
AWS_ACCESS_KEY_ID	AKIARTUZIEISHBD3PHFZ
AWS_SECRET_ACCESS_KEY	(the secret key Revanth gave)
EMAIL_FROM	brave.niat@nxtwave.in
Then republish the app.

Final check before trusting it
If the AWS account is in SES production → trigger a revenue verify/reject and confirm the email lands.
If still in sandbox → first send a test only to a verified address, and get production access before going live to students.
and next tell me one thing don;lt touch any cron jobs mind it here we are just change the brevo to amazon ses so here we need tell me what appnes only
Correct — cron jobs are NOT touched. Here's exactly what happens with this change:

What changes (only this)
One thing only: the inside of sendEmail() in lib/email/brevo.ts.
It stops calling the Brevo API and instead calls Amazon SES.
That's the only real change — just the "delivery pipe".
What is NOT touched
❌ Cron jobs — cron-reminders.ts and routes/cron.ts are not modified. Same schedule, same logic, same timing. They just call sendEmail() — and that function still has the exact same name and inputs, so they don't even notice the swap.
❌ financials.ts — verify/reject email triggers unchanged.
❌ Email content — subjects, body text, templates (revenue-verified.ts, revenue-rejected.ts) unchanged.
❌ Who gets emails — team leader for verify/reject, team members for Day-7 journal reminder — unchanged.
❌ In-app notifications — unchanged.
❌ Any other feature — unchanged.
So the net effect
Item	Before	After
Email provider	Brevo	Amazon SES
When emails send	(cron + verify/reject)	exactly the same
Who receives them	same	exactly the same
Email content	same	exactly the same
Cost	high	much cheaper
In plain words
The cron jobs keep running on the same schedule and keep triggering the same emails. The only difference is that when an email is actually sent, it now goes out through Amazon SES instead of Brevo. Nothing about when, to whom, or what the emails are changes — and no cron code is edited.

The prompt I gave already says explicitly: "Do NOT modify cron-reminders.ts or cron.ts" — so you're safe.