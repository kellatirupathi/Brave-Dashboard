Refactor the BRAVE application routing so season/version becomes part of every authenticated page URL and the URL becomes the primary source of truth for which season’s data is loaded.

Use canonical route structure:

Admin
/admin/season/1.0
/admin/season/2.0
/admin/season/1.0/queue
/admin/season/2.0/queue
/admin/season/2.0/team-requests
/admin/season/2.0/programme
/admin/season/2.0/reports
/admin/season/2.0/config
...

Coordinator
/coordinator/season/1.0
/coordinator/season/2.0
/coordinator/season/2.0/queue
/coordinator/season/2.0/teams
/coordinator/season/2.0/journals
...

Student
/student/season/1.0
/student/season/2.0
/student/season/1.0/projects
/student/season/2.0/leads
/student/season/2.0/journal
/student/season/2.0/leaderboard
...

Implement a central SeasonRouteGuard / SeasonRouter using the existing SeasonProvider, useSeason(), switchTo(), season APIs, and x-brave-season request header.

Important behaviour:

Opening /admin/season/1.0 must load only Season 1 data.
Opening /admin/season/2.0 must load only Season 2 data.
Direct URL, refresh, bookmark, back/forward navigation and shared links must always restore the season from the URL.
Resolve URL 1.0 / 2.0 to the actual internal season ID; do not hard-code 1 = Season 1 forever.
While the URL season and application season are synchronizing, show the existing BRAVE loading animation and do not render stale data.
Set the season first, update the API season getter, invalidate/refetch queries, and only then render the page.
Prevent any temporary Season 1 → Season 2 data flash.
Season switcher must update the URL while keeping the current page. Example:
/admin/season/1.0/queue → switch to 2.0 → /admin/season/2.0/queue.
If that page does not exist for the target season, redirect to that role’s season dashboard.
/admin, /coordinator, and authenticated student root should redirect to the correct canonical season URL based on the saved/default viewing season.
Super-admin/default season selection should determine the initial redirect when no season exists in the URL.
Invalid URLs such as /admin/season/9.0/... must safely redirect to the allowed/default season.
Update sidebar links, cards, buttons, breadcrumbs, redirects, permission checks, page-view tracking and internal navigation to preserve the current season prefix.
Do not leave mixed old links like /admin/queue.
Keep login/auth/public routes season-independent.
Keep the existing role permissions and admin page permissions unchanged.
Maintain Season 1 project flow and Season 2 lead/pipeline flow correctly.
Do not duplicate pages or APIs; create reusable route helpers such as seasonPath(role, seasonVersion, path).
Preserve query-string parameters and IDs, e.g. /admin/season/2.0/chatbot-history?userId=....
Add backward-compatible redirects from existing URLs such as /admin/queue to the appropriate canonical season URL.
Do not break mobile/native routing.

Goal: the browser URL must always clearly show the role + selected season + current page, and loading/refetching must always follow that URL season consistently across the entire application.