Prompt for Replit:

I need you to make the following changes to the BRAVE Dashboard. Do not break any existing admin or coordinator functionality. Apply all changes in one go.

1. Soften the whitelist gate so unknown Forms users auto-become students
In artifacts/api-server/src/routes/auth.ts, inside the POST /api/auth/generate-token handler (and any other place that enforces the whitelist gate / returns 403 "not whitelisted" / "not authorized to access this dashboard"):

Current behavior: if the Forms user is not in the users table AND not in the roster table AND not a bootstrap admin → return 403.
New behavior: if the Forms user passes Forms SSO successfully but is not found anywhere in our DB:
Auto-create a row in the users table with:
formsUserId = the UUID Forms sent
email = the email Forms sent (if available)
firstName / lastName = from Forms payload (fall back to email prefix)
role = "student"
campusId = null (unless we can match by email domain/roster)
teamId = null
Then proceed with normal token issuance.
Keep the bootstrap admin promotion (BOOTSTRAP_ADMIN_FORMS_IDS, BOOTSTRAP_ADMIN_EMAILS) intact and run it AFTER auto-create.
Log the auto-create event in the audit_log table with action like "auto_provisioned_student".
The result must be: every successful Forms login lets the user in. Admins and coordinators continue to work as today; brand-new users land as students with no team.

2. Confirm the four student UI states work correctly
After the change above, these four cases must all work end-to-end:

Case 1 — Student in DB, no team: sidebar shows only Get started + Leaderboard. Root / redirects to /get-started.
Case 2 — Student in DB, with team: sidebar shows Dashboard, Projects, Leaderboard, My Team, Demo Day. Root / shows the Team Dashboard.
Case 3 — Student NOT in DB before login: after auto-creation, behaves exactly like Case 1.
Case 4 — Student NOT in DB then joins a team: after auto-creation + team join, behaves exactly like Case 2.
The sidebar logic in artifacts/brave-dashboard/src/components/sidebar.tsx already handles this correctly via user.teamId. Just verify nothing else gates these pages on roster presence, and remove any leftover roster-only checks that block teamless students from seeing /get-started or /leaderboard.

3. Make "My Team" a full CRUD experience for students
On /team (file: artifacts/brave-dashboard/src/pages/student/team.tsx) and the supporting backend in artifacts/api-server/src/routes/teams.ts and team-flow.ts:

The team leader must be able to:

Create — already works via /get-started (keep as-is).
Read — already works (keep as-is).
Update — edit team name, description, tagline, logo. Add a backend PATCH /teams/:id permission for the team leader (currently admin-only) and a frontend "Edit Team" dialog on /team.
Delete — add a DELETE /teams/:id route. Allowed only for the team leader if the team has no verified revenue entries and no submitted/verified order book entries. On success, all members are detached, pending invitations / join requests / leave requests are cancelled, and the team row is soft-deleted (or hard-deleted if you prefer). Add a "Delete team" button on /team with a confirmation dialog.
Member management — leader can add members (by invite code or invitation), remove members, and transfer leadership (already partially exists — make sure the buttons are visible on /team for the leader role).
Non-leader members on /team:

Can read everything.
Can only request to leave (already exists — keep).
Cannot edit, delete, or remove other members.
4. Make "Projects" full CRUD for students
On /projects and /projects/:id (files: artifacts/brave-dashboard/src/pages/student/projects/list.tsx and detail.tsx) and backend artifacts/api-server/src/routes/projects.ts:

Any active team member must be able to:

Create — already works (POST /projects). Keep.
Read — already works. Keep.
Update — already works (PATCH /projects/:id). Keep. Make sure the "Edit project" button is visible to all team members, not just leader.
Delete — add DELETE /projects/:id endpoint. Allowed only if the project has no submitted or verified order book / revenue entries. Cascade-delete only draft entries. Add a "Delete project" button with confirmation on the project detail page.
Order book entries and revenue entries (already CRUD-capable on /projects/:id) — keep as-is.

5. Acceptance checklist (please verify before reporting done)
 A brand-new Forms user (never seen before) can log in and lands on /get-started.
 An existing student with no team sees only Get started + Leaderboard in sidebar.
 An existing student with a team sees Dashboard, Projects, Leaderboard, My Team, Demo Day.
 Admin and coordinator login still works exactly as before — no regressions.
 Team leader can edit and delete their own team (when conditions allow).
 Team member can create, edit, and delete projects (when conditions allow).
 All destructive actions show a confirmation dialog with what will happen.
 Audit log records team-delete, project-delete, and auto-provisioned-student events.
Please implement all of the above and then run a quick smoke test from the dev login page covering all four student cases.

how admin can identify new registred student and already existing db students how can admin identify tell me only
Here are the ways admin can tell auto-provisioned (walk-in) students apart from pre-known students — pick one or combine them:

Direct identifiers (need a small schema/UI tweak)
#	How	What admin sees
1	Add a provisionedVia column on users table with values like roster, csv_import, manual, auto_forms_sso	A "Source" column on /admin/users page showing exactly how each user got in
2	Add a boolean isAutoProvisioned flag set to true only when created by the Forms gate	A "Walk-in" / "Auto-provisioned" badge next to the name on /admin/users
3	Filter dropdown on /admin/users — "All / Roster / Manual / Auto-provisioned"	Admin clicks a filter to see only walk-ins
Signals already available in current schema (no migration needed)
#	How	What admin sees
4	Cross-check roster table — if a student's formsUserId is NOT in the roster table, they're a walk-in	Add a "On roster" column / icon (✅ / ❌) on /admin/users
5	Missing fields signal — auto-provisioned users have campusId = null, niatId = null, batchSection = null (because Forms didn't supply them)	Highlight rows with empty Campus / NIAT ID — those are walk-ins
6	createdAt timestamp — walk-ins are created at login time, pre-known students are created at roster/import time	Sort by created date; recent + post-deployment dates = walk-ins
7	Audit log — every auto-create writes an entry with action auto_provisioned_student	/admin/audit-log filter by that action shows the full list of walk-ins with timestamps
Recommended combo (cleanest UX)
Add the provisionedVia column (option 1) — single source of truth.
Show a "Source" column + filter on /admin/users (option 3).
Write the audit log entry on every auto-create (option 7) — so there's a permanent trail.
Optionally add a bulk-action button "Move selected to roster" so admin can promote walk-ins into the official roster after verifying them.
That way the admin's /admin/users page tells them at a glance: "this row came from the official roster" vs "this person walked in via Forms and we should review them."