Update **only the Admin Dashboard page for BRAVE 2.0** to match the attached reference image as closely as possible.

### Critical rules

* **Do NOT change the existing BRAVE 1.0 Dashboard UI, layout, styling, logic, components, or behavior.**
* The existing Dashboard must remain exactly as it is when **Viewing 1.0**.
* Apply this redesign **only when the selected/viewing season/version is 2.0**.
* Reuse the existing APIs, queries, calculations, permissions, routing, and real backend data.
* Do not create mock/static production data.
* Do not modify the sidebar as part of this task.
* Do not change sidebar colors or navigation structure.
* Focus only on the **Dashboard main content area** shown to the right of the sidebar.
* Do not change any other Admin, Coordinator, or Student pages.
* No backend/database changes unless absolutely required; this should primarily be a frontend Dashboard redesign.
* Preserve existing loading, error, permission, season-switching, and responsive behavior.

---

# BRAVE 2.0 Dashboard Design

Create the Dashboard main content exactly in this visual structure.

## 1. Page Header

At the top-left of the dashboard content:

**National Command Center**

Below it:

**Program-wide health, pending work, and live activity at a glance.**

Use:

* large bold dark maroon heading
* smaller muted grey subtitle
* generous spacing
* clean minimal appearance

Keep the existing season/viewing selector and Feedback controls at the top-right.

Do not redesign the global season-switching functionality.

---

# 2. Top Revenue KPI Cards

Directly below the header, create **4 equal-width KPI cards in one horizontal row**.

### Card 1

Icon: Trophy / verified achievement
Title:

**VERIFIED REVENUE**

Main value:
**₹{verifiedRevenue}**

Supporting text:

**Counts toward leaderboard**

Include a small red/maroon sparkline on the right side of the card.

---

### Card 2

Icon: Order book / briefcase
Title:

**ORDER BOOK**

Value:

**₹{orderBook}**

Supporting text:

**Committed pipeline**

Small maroon/brown sparkline on the right.

---

### Card 3

Icon: Clock
Title:

**PENDING REVENUE**

Value:

**₹{pendingRevenue}**

Supporting text:

**Awaiting review**

Use orange for the main metric/accent.

Include a small orange sparkline.

---

### Card 4

Icon: X / rejected indicator
Title:

**REJECTED REVENUE**

Value:

**₹{rejectedRevenue}**

Supporting text:

**Rejected entries**

Small grey/red sparkline.

---

### KPI Card Styling

Use:

* white background
* subtle warm off-white page background
* approximately 12–16px card radius
* very subtle border
* extremely soft shadow
* compact padding
* small uppercase title
* large bold number
* muted supporting description
* sparkline positioned at the lower/right side
* no oversized icons
* clean SaaS/enterprise appearance

---

# 3. Program Health Snapshot

Below the revenue cards create one large white card titled:

**PROGRAM HEALTH SNAPSHOT**

Use a small activity/pulse icon before the heading.

The card contains **8 metrics arranged as 4 columns × 2 rows**.

Use very thin divider lines between cells.

### Row 1

#### Active Teams

Icon: users
Value: `{activeTeams}`
Label:

**ACTIVE TEAMS**

Subtitle:

**Across {campusCount} campuses**

#### Demo Day Eligible

Icon: rocket
Value: `{demoDayEligible}`
Label:

**DEMO DAY ELIGIBLE**

Subtitle:

**Teams crossing ₹2L**

#### GRIT Miles

Icon: flag
Value: `{gritMiles}`
Label:

**GRIT MILES**

Subtitle:

**Highest milestone**

#### GRIT Miles Achieved

Icon: medal
Value: `{gritMilesAchieved}`
Label:

**GRIT MILES ACHIEVED**

Subtitle:

**Teams at a GRIT level**

---

### Row 2

#### Pending Reviews

Icon: clock
Value: `{pendingReviews}`

**PENDING REVIEWS**

Subtitle:

**Nothing overdue**

Use the existing overdue wording dynamically where applicable.

#### Campuses

Icon: building
Value: `{campusCount}`

**CAMPUSES**

Subtitle:

**Program-wide**

#### Teams Awaiting Approval

Icon: checklist/users
Value: `{teamsAwaitingApproval}`

**TEAMS AWAITING APPROVAL**

Subtitle:

**Team requests**

#### Roster Requests

Icon: user-plus
Value: `{rosterRequests}`

**ROSTER REQUESTS**

Subtitle:

**New-user access**

---

Use subtle pastel icon backgrounds similar to the reference:

* soft rose
* orange
* mint
* light blue

Keep the actual product palette restrained and professional.

---

# 4. Main Dashboard Content Layout

Below Program Health Snapshot use a **two-area layout**:

### Main left area

Approximately **75–78% width**

Contains:

1. Activity Overview
2. Revenue Pipeline
3. Campuses Needing Attention

### Right column

Approximately **22–25% width**

Contains vertically:

1. This Week's Journal Coverage
2. Top Campuses

Maintain consistent gaps between cards.

---

# 5. Activity Overview Card

Create a large card titled:

**ACTIVITY OVERVIEW**

At the top-right place a dropdown:

**Last 7 Days**

Reuse real activity data.

Inside the card create **3 equal sections horizontally**:

### Team Requests

Large metric:

`{teamRequests}`

Below create a minimal small line chart.

### Reviews Completed

Metric:

`{reviewsCompleted}`

Below create a minimal orange line chart.

### Roster Requests

Metric:

`{rosterRequests}`

Below create a minimal green line chart.

Use subtle separators between the three chart areas.

Keep chart axes very minimal and light.

Do not visually overload the dashboard.

If the existing data is zero, show a flat zero line exactly like the reference.

---

# 6. Revenue Pipeline Card

Place this card to the right of Activity Overview but still inside the main left content area.

Title:

**REVENUE PIPELINE (₹)**

Display a funnel-style visualization with 3 stages:

### Stage 1

Dark maroon

**Order Book**

₹{orderBook}

Percentage of pipeline

### Stage 2

Orange

**Pending Revenue**

₹{pendingRevenue}

Percentage

### Stage 3

Green

**Verified Revenue**

₹{verifiedRevenue}

Percentage

Under the funnel display:

**Conversion    {conversionPercentage}%**

Use existing values to calculate the percentages safely.

Handle zero totals without NaN or Infinity.

The funnel should look similar to the reference:

* wide top
* narrower middle
* narrow bottom
* labels centered inside each level

---

# 7. This Week's Journal Coverage

Place this card at the **top of the right column**.

Header:

**THIS WEEK'S JOURNAL COVERAGE**

At the top-right show status such as:

🔴 **Critical**

using existing journal health logic.

Large primary metric:

**{coveragePercentage}%**

Next to/below it:

**{submittedTeams} of {totalTeams} teams**

**across {campusesCovered} campuses**

Below place a subtle horizontal progress bar.

Then display the 4 journal states in a **2 × 2 grid**:

### Submitted

Green dot
`{submitted}`
**SUBMITTED**

### Pending

Neutral/light orange or grey dot
`{pending}`
**PENDING**

### Silent

Red dot
`{silent}`
**SILENT**

### Never Logged

Grey/blue-grey dot
`{neverLogged}`
**NEVER LOGGED**

At the bottom place two actions side-by-side:

Outlined button:

**View heatmap**

Primary red button:

**Bulk-remind {silentCount} silent teams**

Reuse the existing button behavior and permission checks.

Do not create duplicate reminder logic.

---

# 8. Top Campuses

Place immediately below Journal Coverage in the right column.

Header:

Trophy icon

**TOP CAMPUSES**

Right side:

**View all**

Show the first 5 campuses.

Each row should have:

Rank badge | Campus name | Teams count | Revenue

Example visual structure:

**1**  AMET University
0 teams                    ₹0

**2**  Ajeenkya DY Patil University
0 teams                    ₹0

**3**  Annamacharya University
0 teams                    ₹0

**4**  Aurora Deemed University
0 teams                    ₹0

**5**  Chaitanya – Deemed to be University
0 teams                    ₹0

Use:

* gold accent for rank 1
* neutral grey for remaining ranks
* thin row separators
* no large backgrounds
* compact readable rows

Reuse the existing Top Campuses data and navigation.

---

# 9. Campuses Needing Attention

At the bottom of the main left area create a full-width card titled:

**CAMPUSES NEEDING ATTENTION**

Use an orange building/alert icon.

Show compact horizontal status cards such as:

**{overdueReviews} Overdue reviews**

**{teamsAwaitingApproval} Teams awaiting approval**

**{rosterRequestsPending} Roster requests pending**

At the far right add:

**View all campuses →**

Reuse existing attention/campus data.

If more useful existing campus-attention metrics already exist, continue using those rather than introducing fake calculations.

---

# Visual Styling

Match the attached reference closely.

Main content background:

* very soft warm white / cream
* approximately `#FCFAF7` style tone

Cards:

* white
* subtle warm-grey border
* 12–16px border radius
* very light shadow
* no heavy gradients
* no excessive glass effects

Primary brand:

* deep BRAVE maroon
* red
* warm gold
* orange
* limited green for positive states

Typography:

* use the dashboard's existing font family
* strong dark-maroon page title
* small uppercase KPI headings
* clear bold numbers
* muted secondary descriptions
* excellent whitespace

Avoid:

* excessive colors
* oversized shadows
* very large icons
* unnecessary gradients
* excessive animation
* decorative elements that reduce readability

---

# Responsive Behaviour

Desktop should match the reference first.

For narrower screens:

* KPI cards can become 2 columns
* Program Health Snapshot can become 2 columns
* Activity Overview and Revenue Pipeline can stack
* right column should move below the primary content
* maintain readable spacing
* no horizontal overflow

---

# Implementation Requirement

Use the existing season/version context.

Conceptually:

```tsx
if (selectedSeason/version === "2.0") {
  return <AdminDashboardV2 />;
}

return <ExistingAdminDashboard />;
```

Adapt this to the repository's existing season architecture instead of adding an unrelated version-state system.

**Do not duplicate API fetching unnecessarily.** Reuse existing hooks/query results where possible.

The final result must satisfy this behavior:

**Viewing 1.0 → existing Dashboard remains exactly unchanged.**

**Viewing 2.0 → show this new National Command Center dashboard design matching the attached reference image.**
