IMPORTANT: Make ONLY the changes described below. Do NOT touch, refactor, or modify any other feature, file, route, or behaviour. Keep BRD relevancy, BRD uniqueness, all admin/coordinator/student pages, and every other subsystem exactly as they are. These are backend AI changes only.

I need two changes in the BRAVE dashboard's Gemini AI logic (artifacts/api-server/src/lib/ai/):

CHANGE 1 — In the BRD relevancy prompt (brd-prompt.ts), make "How AI is used" OPTIONAL.

Currently the BRD auditor treats "How AI is used" as a mandatory scored section and lowers the score + shows a ❌ missing AI explanation finding when it's absent. I want it to be fully optional:

Its absence must NOT reduce the BRD relevancy score.
Its absence must NOT be counted as a missing section.
It must NEVER appear as a finding (treat it exactly like invoices are already treated — zero point cost, never mentioned).
If AI usage IS described in the BRD, you may still note it positively — only stop penalising its absence.
This is a prompt-only change. Do not change the score for any other section (owner details, problem, solution, deliverables, pricing, client details, sign-off, team must all still count and still be reported as before).
CHANGE 2 — Merge journal analysis + journal reel scan into a SINGLE Gemini API call.

Right now, every weekly journal triggers TWO separate Gemini calls: one for journal analysis (analyse-journal.ts, ~30s after submit) and one for the reel scan (analyse-journal-reel.ts, ~35s after submit). Both read the exact same journal content, so combine them into ONE call to halve journal Gemini usage.

Requirements:

Create one merged prompt that returns a single combined JSON: { "analysis": {...}, "reel": { "worthy", "bucket", "reason", "script" } }. The analysis part must be identical in shape/behaviour to the current journal analysis (what_we_did, blockers with priority triage, next_week, primary_category, overall_summary). The reel part must be identical in behaviour to the current reel scan (judge worthy vs not-worthy strictly, using this team's last 8 previous journals as context, and only write a script if worthy).
analyseJournal(id) should make ONE generateBrdAnalysis call, parse both halves, and write BOTH the analysis fields (aiAnalysis, blockerPriority) AND the reel fields (reelWorthy, reelBucket, reelScript, reelReason, reelAnalysedAt) in one guarded UPDATE. Keep the existing stale-run guard (submittedAt snapshot), the manual-priority override (blockerPriorityManual), and never touch blockerStatus/blockerNote.
Worthy scripts must still be mirrored into the reel_scripts table (dedup-safe, onConflictDoNothing), exactly like before.
Remove the now-redundant separate reel scheduling: every place that currently calls scheduleJournalReelScan should just rely on the merged scheduleJournalAnalysis. The admin /admin/journals/:id/reel-scan endpoint must keep working but should re-run the merged analysis (point it at runJournalAnalysisNow).
Remove the separate reel catch-up sweep at boot in index.ts — the single journal-analysis catch-up now covers both.
DELETE the dead/redundant files after repointing their imports: analyse-journal-reel.ts, journal-reel-scheduler.ts, journal-reel-prompt.ts, and also DELETE the old dormant batch reel generator generate-reels.ts + reels-prompt.ts (nothing schedules them anymore — the daily reel-gen cron was already removed).
routes/reels-scripts.ts currently imports reelDedupeKey from generate-reels.ts and REEL_BUCKETS from reels-prompt.ts — repoint those imports so the reels library page keeps working (get reelDedupeKey from the merged journal analysis module and the reel buckets constant from the journal prompt module). Do NOT change the reels library UI or its behaviour.
Both changes are prompt/logic only — no database schema changes. They apply to newly submitted or re-analysed items; existing rows keep their data until re-analysed.

Again: do not modify anything outside what is described above.