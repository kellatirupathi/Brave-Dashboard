Update the BRAVE Dashboard so that NIAT IDs are displayed instead of email IDs everywhere users are shown to other users in the UI, with email as a fallback when NIAT ID is missing. Do NOT remove email from forms, CSV exports, delete-confirmation dialogs, or the roster admin page — those keep email as-is.

Rule everywhere we apply this:
display value = niatId ?? email

Stack context: pnpm monorepo. OpenAPI-driven (lib/api-spec/openapi.yaml → Orval codegen → lib/api-zod + lib/api-client-react). Express 5 API in artifacts/api-server. React + Vite + wouter frontend in artifacts/brave-dashboard. The users table in lib/db/src/schema/brave.ts already has a niat_id column — no DB migration needed.

Make the changes in this exact order:

=== STEP 1 — OpenAPI spec ===
Edit lib/api-spec/openapi.yaml. Add a nullable niatId field (type: ["string", "null"]) to each of these schemas. Place it next to the existing email field. Do NOT mark it required.

1. AuthUser → add niatId
2. User (the user response object used by admin list) → add niatId
3. TeamMember → add niatId
4. TeamInvitation → add inviteeNiatId
5. TeamJoinRequest → add requesterNiatId
6. TeamLeaveRequest → add requesterNiatId
7. The search-students result schema returned by GET /teams/search-students (or whatever the campus student search operation returns) → add niatId

=== STEP 2 — Server projections ===
Update artifacts/api-server/src/routes/teams.ts:
- In getTeamWithStats, the memberDetails projection (around line 92-104) must include niatId: user?.niatId ?? null
- In the search-campus-students handler, include niatId from the users row in the response
- In list invitations, include inviteeNiatId from the invitee user
- In list join-requests, include requesterNiatId from the requester user
- In list leave-requests, include requesterNiatId from the requester user

Update artifacts/api-server/src/routes/auth.ts:
- The GET /auth/user response must include niatId on the AuthUser shape
- The session payload stored on login/callback must persist niatId so refreshIfExpired keeps it

Update artifacts/api-server/src/routes/admin.ts:
- The admin listUsers handler must include niatId on each user row

=== STEP 3 — Regenerate codegen ===
Run: pnpm --filter @workspace/api-spec run codegen

After regen, verify lib/api-zod/src/index.ts still only re-exports ./generated/api and AuthUser. Do NOT re-export ./generated/types — that causes duplicate-export build errors (this is a known pitfall in replit.md).

=== STEP 4 — Frontend display swaps ===
Apply these 7 single-line edits exactly:

File: artifacts/brave-dashboard/src/pages/student/team.tsx
- Line 680 (search-students dialog): change {s.email} to {s.niatId ?? s.email}
- Line 742 (team members list): change {member.email} to {member.niatId ?? member.email}
- Line 907 (pending invitations card): change {inv.inviteeEmail} • Invited by {inv.inviterName} to {(inv.inviteeNiatId ?? inv.inviteeEmail)} • Invited by {inv.inviterName}
- Line 952 (join requests card): change {jr.requesterEmail} to {jr.requesterNiatId ?? jr.requesterEmail}

File: artifacts/brave-dashboard/src/pages/admin/team-detail.tsx
- Line 421 (member list): change {m.email} to {m.niatId ?? m.email}

File: artifacts/brave-dashboard/src/pages/admin/users.tsx
- Line 488 (user directory row): change <Mail className="w-3 h-3" /> {user.email} to <Mail className="w-3 h-3" /> {user.niatId ?? user.email}

File: artifacts/brave-dashboard/src/components/navbar.tsx
- Line 91 (logged-in user pill): change {user.firstName || user.email} to {user.firstName || user.niatId || user.email}

=== DO NOT CHANGE ===
- artifacts/brave-dashboard/src/pages/auth/not-on-roster.tsx — email is a form input
- artifacts/brave-dashboard/src/pages/admin/roster.tsx — this page administers both fields; keep email column, CSV export, form inputs untouched
- artifacts/brave-dashboard/src/pages/admin/users.tsx lines 296, 412, 605 — CSV export, create-user form field, delete-confirmation dialog must keep email
- artifacts/brave-dashboard/src/pages/auth/dev-login.tsx — DEV-only screen
- Any memberInitials(...) or nameInitials(...) calls — they use email only as a 2-char avatar fallback, not as display text

=== VERIFICATION ===
After all changes:
1. Run: pnpm run typecheck — must pass with zero errors.
2. Run the API server and frontend locally.
3. Visual smoke test on these pages — each must show NIAT ID where it previously showed email, and fall back to email when niatId is null:
   - /team (members section, invite dialog, pending invitations card, join requests card)
   - /teams/:id and /admin/teams/:id (team detail member list)
   - /admin/users (user directory secondary line)
   - Top navbar pill on any authenticated page
4. Confirm /admin/roster still shows both Email column and NIAT ID column unchanged.
5. Confirm CSV export from /admin/users still includes the Email column.
6. Confirm forms (create user, edit roster, not-on-roster) still accept and display email as before.

Do not introduce any new dependencies. Do not edit generated files in lib/api-zod/src/generated or lib/api-client-react/src/generated by hand — they must come from codegen only.
