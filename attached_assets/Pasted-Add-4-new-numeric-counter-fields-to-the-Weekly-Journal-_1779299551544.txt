Add 4 new numeric counter fields to the Weekly Journal feature, surface them in the Campus Insights page, and update the admin sidebar.

==========================================================
PART 1 — JOURNAL: 4 new numeric counter fields
==========================================================

Fields (all integers, min 0, default 0):
  - clientsVisited        (DB col: clients_visited)
  - activeConversations   (DB col: active_conversations)
  - projectsStarted       (DB col: projects_started)
  - projectsClosed        (DB col: projects_closed)

Required on create (default to 0 if omitted). Optional on update.

1) DB schema — lib/db/src/schema/brave.ts
   In `weeklyJournalsTable` add the 4 integer columns, each
   `.notNull().default(0)`, placed after `nextWeekPlan`.
   Then run `pnpm --filter @workspace/db run push`. Existing rows
   backfill to 0 via the default.

2) API route — artifacts/api-server/src/routes/journals.ts
   Add the 4 fields in all six spots:
   a) Create body Zod schema: each as z.number().int().min(0).default(0).
   b) Insert values for upsert create + update branches.
   c) Select projection for listing.
   d) Update body Zod schema: each as z.number().int().min(0).optional().
   e) Update field-copy block: copy each if `!== undefined`.
   f) Audit-log before/after snapshots.

3) Frontend type + API helper — artifacts/brave-dashboard/src/lib/progress-api.ts
   - Add the 4 fields (typed `number`) to the `WeeklyJournal` type.
   - Add to the `createJournal({...})` body type (all `number`, optional).
   - Add to the `updateJournal(id, {...})` patch type (all `number | undefined`).

4) Student journal page — artifacts/brave-dashboard/src/pages/student/journal.tsx
   - Add four `useState<number>(0)` hooks.
   - Prefill from existing journal on load.
   - Include in submit payload.
   - Render four small number inputs (label + Input type="number" min=0 step=1)
     in a 2-column grid, ABOVE the "What we did this week" textarea, under a
     small heading "This week's numbers".
   - In the past-journal read view, show a compact stat strip
     ("Clients visited: 3 · Active conversations: 5 · Started: 2 · Closed: 1")
     above the "What we did" text.

5) Edit dialog — artifacts/brave-dashboard/src/components/journal-edit-dialog.tsx
   Mirror exactly the same state + inputs + prefill + submit changes.

6) Admin journals page — artifacts/brave-dashboard/src/pages/admin/journals.tsx
   On each journal card, render a compact stat strip directly under the
   team/week header and ABOVE the "What we did" text:
     "Clients visited: 3  ·  Active conversations: 5  ·
      Projects started: 2  ·  Projects closed: 1"
   Use bg-muted/40 rounded-md px-3 py-2 styling, label in
   text-muted-foreground text-xs, value in font-semibold text-sm.
   Always render (even when all zeros) so silent teams are visible.

7) Coordinator journals page — artifacts/brave-dashboard/src/pages/coordinator/journals.tsx
   Same display change as the admin page.

Validation: server rejects negative / non-integer with 400. Empty input on
the frontend coerces to 0, never NaN. No OpenAPI/Orval regeneration
required (journals use the hand-rolled progress-api.ts).

==========================================================
PART 2 — CAMPUS INSIGHTS: add the 4 new columns
==========================================================

In the Campus Insights page (the hidden /admin/campus-insights page),
add the 4 journal counters to BOTH tables.

VIEW A — "All campuses" (each row = one campus). New columns appended
after "Journals submitted":
   Campus name | No. of teams | Journals submitted |
   Clients visited | Active conversations | Projects started | Projects closed |
   Verified revenue (count) | Rejected revenue (count)

VIEW B — specific campus (each row = one team). New columns appended
after "Journal weeks submitted":
   Team name | Journal weeks submitted (X / total) |
   Clients visited | Active conversations | Projects started | Projects closed |
   Order book submitted (count) | Verified revenue (count) |
   Rejected revenue (count) | Total verified amount (₹)

Each new column is the SUM across the relevant rows in weekly_journals
(per campus for VIEW A, per team for VIEW B). Compute server-side in
the existing /api/admin/campus-insights endpoints — one SQL aggregation
per view, no N+1. Display the four values as plain integers, right-aligned.

CSV exports (both "Campus-wise" and "All campuses (single CSV)") must
include the four new columns in the same order as the table, with
headers: "Clients visited", "Active conversations", "Projects started",
"Projects closed".

Sort tiering on the Campus Insights page is unchanged.

==========================================================
PART 3 — ADMIN SIDEBAR
==========================================================

In the admin sidebar (artifacts/brave-dashboard/src/components/sidebar.tsx
or wherever the admin nav is defined):

1) Add a new top-level item "Campus Insights" linking to
   /admin/campus-insights, positioned BETWEEN "Communications" and
   "Config". Use an appropriate lucide-react icon (e.g. BarChart3 or
   LineChart).

2) Convert "Communications" into a collapsible group with THREE
   sub-items (in this order):
     - Announcements   → /admin/announcements
     - Audit Log       → /admin/audit-log
     - Feedback        → /admin/feedback
   "Audit Log" and "Feedback" were previously hidden / not in the
   sidebar — they are now exposed under Communications. All three
   sub-routes remain admin-only.

Order of admin sidebar (top → bottom):
   Dashboard
   Review Queue
   Programme (existing group)
   Performance (existing group)
   Demo Day
   Setup (existing group)
   Communications
       ├─ Announcements
       ├─ Audit Log
       └─ Feedback
   Campus Insights      ← NEW, between Communications and Config
   Config
   Resources

ACCEPTANCE
- Students see the 4 number inputs on the journal form; submitted values
  persist and reappear on edit.
- Admin & coordinator journal views render the compact stat strip per row.
- /admin/campus-insights VIEW A and VIEW B both show the 4 new columns
  with correct sums; CSV exports include them.
- Admin sidebar shows Campus Insights between Communications and Config,
  and Communications contains Announcements, Audit Log, and Feedback.
- `pnpm run typecheck` is clean.

NON-GOALS
- No changes to heatmap, reminders, or email templates.
- No new routes besides what's already specified.
- Campus Insights page remains admin-only and is reachable from the new
  sidebar item (it is no longer URL-only — that earlier "hidden" rule is
  overridden by this update).
