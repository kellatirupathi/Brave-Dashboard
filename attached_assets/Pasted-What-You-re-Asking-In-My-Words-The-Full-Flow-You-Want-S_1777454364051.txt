What You're Asking — In My Words
The Full Flow You Want
Step 1 — On any student login (Forms SSO):

Take their Forms user ID and check against the roster table first.

Step 2 — Two paths based on roster lookup:

Path A — Roster match found ✅
Copy the roster details (real email, NIAT ID, full name, campus, batch) into the users table one time only
After that, send the student to their normal destination (dashboard if they have a team, get-started if they don't)
Do NOT re-copy on later logins (one-shot)
Path B — No roster match found ❌
Leave the synthetic data as-is (sso_<id>@forms.local, empty NIAT ID, etc.)
Force redirect them to the Profile / Edit Profile page immediately on login
They CANNOT skip it — profile fields (name, email, NIAT ID, etc.) become mandatory
They must fill in the details and click "Save Changes"
Once saved, the system then routes them based on team status:
If they already have a team → go to Dashboard (/)
If no team → go to Get Started page (/get-started)
The Two Key Behaviors You're Stressing
Mandatory profile completion gate — any student whose details are still missing/synthetic must complete their profile before being allowed anywhere else in the app. They land on the Edit Profile page the moment they log in, every time, until details are filled.

One-time auto-fill from roster — if the roster already has their data, the system silently uses it (so they skip the profile-fill step entirely and don't get blocked).

The Outcome You Want
User scenario	What happens on login
In roster, fresh login	Roster data auto-copied into users → goes straight to dashboard / get-started
In roster, already logged in before	Existing users data is used → goes to dashboard / get-started
NOT in roster, profile empty	Forced to /profile → must fill mandatory fields → Save → then dashboard / get-started
NOT in roster, profile already filled	Goes straight to dashboard / get-started
Did I understand it correctly?

If yes, the implementation will touch:

lib/db/src/forms-auth.ts — add roster lookup + one-time copy in createOrGetUserByFormsId
artifacts/brave-dashboard/src/App.tsx — add a "profile incomplete" gate that redirects to /profile until mandatory fields are filled
artifacts/brave-dashboard/src/pages/profile.tsx — make name/email/NIAT ID mandatory and route based on team status after save
Confirm and I'll implement it.