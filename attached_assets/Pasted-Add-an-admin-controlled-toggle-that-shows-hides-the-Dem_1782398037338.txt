Add an admin-controlled toggle that shows/hides the "Demo Day" menu item in the student sidebar.

Mirror the existing resourcesEnabledForStudents pattern exactly. Do NOT touch or overload the existing gritMilesMenuEnabled flag — create a brand-new, independent flag. Demo Day and GRIT Miles are separate sidebar items now.

1. DB schema — lib/db/src/schema/brave.ts
In the programmeConfig table, add a new boolean column right next to resourcesEnabledForStudents / the GRIT flags:

demoDayMenuEnabled: boolean("demo_day_menu_enabled").notNull().default(true),
Default true (Demo Day visible unless an admin turns it off). Then push the schema: pnpm --filter @workspace/db run push.

2. API — artifacts/api-server/src/routes/grit-config.ts
Carry the new flag through exactly like gritMilesMenuEnabled is already carried:

Add demoDayMenuEnabled: z.boolean().optional() to the UpdateBody Zod schema.
Return demoDayMenuEnabled: row.demoDayMenuEnabled in the student-readable GET /grit-config response, the admin GET /admin/grit-config response, and the PUT /admin/grit-config return value.
In the PUT handler, add if (parsed.data.demoDayMenuEnabled !== undefined) patch.demoDayMenuEnabled = parsed.data.demoDayMenuEnabled;
3. Admin Config page — artifacts/brave-dashboard/src/pages/admin/config.tsx
Add a new <Switch> row in the GRIT/Demo Day settings area, wired to demoDayMenuEnabled via the admin grit-config query + mutation already used there. Label it "Show Demo Day in student sidebar" with helper text like "When off, students won't see the Demo Day menu item." Match the styling of the existing toggles (e.g. "Demo Day Applications Open").

4. Student sidebar — artifacts/brave-dashboard/src/components/sidebar.tsx
The student nav reads the grit-config. Gate the Demo Day item (currently around line 277, in the hasTeam branch of navItems.student) behind the flag, identical to how the Resources item is gated just below it:

...(demoDayMenuVisibleForStudent
  ? [{ name: "Demo Day", href: "/demo-day", icon: Rocket, isNew: true }]
  : []),
Read demoDayMenuEnabled from the student grit-config query (default to true while loading so it doesn't flicker out). Only apply this in the student nav — leave the admin "Demo Day Submissions" item untouched.

5. Route guard — artifacts/brave-dashboard/src/App.tsx
So a student can't reach the page by typing /demo-day directly when it's hidden: when demoDayMenuEnabled is false and the user's role is student, redirect /demo-day to / (or render not-found). Don't affect admin/coordinator access.

Keep everything additive and isolated. Run pnpm run typecheck and confirm it passes before finishing.