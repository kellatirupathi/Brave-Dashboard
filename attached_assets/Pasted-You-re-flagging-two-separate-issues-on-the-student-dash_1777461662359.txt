You're flagging two separate issues on the student dashboard for "BizBridge":

Issue 1 — National Rank & Campus Rank are empty (BUG)
Currently both rank cards show:

National Rank: # — (placeholder dash)
Campus Rank: # — (placeholder dash)
You want them to display the real numeric rank of the team — calculated from the leaderboard:

National Rank = the team's position when all teams across all campuses are ranked by verified revenue (e.g. #42)
Campus Rank = the team's position when only teams in the same campus (NIAT - KKH) are ranked (e.g. #3)
So instead of # —, the user should see actual numbers like #42 (National) and #3 (Campus), pulled from the same leaderboard logic that already powers the /leaderboard page.

Issue 2 — Demo Day Eligible badge + notification bell layout (UI BUG)
Currently the layout shows:


BizBridge          🔔(17)               [✅ Demo Day Eligible!]
No tagline set      ↑                    ↑
                  middle of page         far right
The notification bell (with the red 17 badge) is sitting in the middle of the header, and the "Demo Day Eligible!" green button is on the far right. This looks misaligned and wrong.

You want it fixed to:


BizBridge                     [✅ Demo Day Eligible!]   🔔(17)
No tagline set                       ↑                   ↑
                              left of bell          fixed far right
Specifically:

Notification bell (🔔) must be fixed on the right side of the header
"Demo Day Eligible!" badge/button should sit immediately to the LEFT of the notification bell (not floating in its own corner)
They should be a single right-aligned cluster, with the bell as the rightmost element
Summary of What Needs Fixing
#	Issue	Type	Location
1	National Rank shows # — instead of real number	Backend bug — dashboard API is not returning rank	artifacts/api-server/src/routes/dashboard.ts + frontend dashboard page
2	National Rank shows # — instead of real number	Same as above (campus filter)	Same file
3	Notification bell sitting in middle, Demo Day badge floating far right	Frontend layout bug	artifacts/brave-dashboard/src/pages/student/dashboard.tsx (header section)
