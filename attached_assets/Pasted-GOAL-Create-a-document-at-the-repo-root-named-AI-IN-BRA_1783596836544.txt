GOAL
Create a document at the repo root named AI_IN_BRAVE_PROGRAM.md that explains
HOW THE PROGRAM TEAM LEVERAGED AI to design, build, and run the BRAVE program
and its dashboard. This is program-side only (NOT how students used AI). It will
be reviewed by Divyansh, so keep it clear, factual, and concise. Do NOT change
any code or existing feature — this task only creates the markdown document.

USE REAL VALUES (pull them from THIS repo, don't invent):
- Development timeline = first commit date -> last commit date (git log).
- Total commits (git rev-list --count HEAD).
- Codebase size = lines of TS/TSX excluding node_modules and generated files.
- Backend breadth = number of API route files in artifacts/api-server/src/routes,
  number of DB tables (count pgTable( in lib/db/src/schema).
- Frontend breadth = 3 role-based dashboards (Student, Coordinator, Admin),
  number of pages and components under artifacts/brave-dashboard/src.
Put these in a small metrics table. If a value can't be derived from the repo
(e.g. team size, "Nx faster" multiplier), leave a clear [fill] placeholder —
never fabricate numbers.

STRUCTURE
1. One-line purpose + scope note (program-side AI usage, not students).
2. Short executive summary: AI was used at two levels — to BUILD the platform,
   and RUNNING INSIDE the platform.
3. "AI to build the platform" — the real metrics table above + 2-3 lines on how
   3 dashboards / this much breadth were delivered fast with AI-assisted
   development, and how quickly new requirements were shipped (iteration speed).
4. "AI running inside the program" — describe each real AI capability in the
   codebase, each with a one-line impact:
   - BRD AI analysis: Gemini scores every revenue BRD for relevancy + uniqueness
     before human verification (see lib/ai/analyse-brd.ts).
   - Weekly journal AI analysis: extracts/prioritises blockers, flags at-risk
     teams, powers the Success Coach -> COS -> Admin escalation.
   - AI-generated Instagram reel scripts from strong journal moments (marketing).
   - In-product AI chatbot over a BRAVE knowledge base.
5. AI / technology stack table (Gemini 2.5 Flash for analysis; Cerebras Llama
   3.1 8B / Cloudflare Workers AI for chatbot; React+Vite / Express+Postgres).
6. A short "open items" checklist for any remaining [fill] values.

CONSTRAINTS
- Only create/update the markdown file. No code changes, no new dependencies.
- Verify every number against the actual repo before writing it.
- Keep it tight and skimmable (headings, bullets, small tables).

DELIVERABLE
The finished AI_IN_BRAVE_PROGRAM.md at the repo root, with all repo-derived
values filled in and only genuinely-unknown values left as [fill].