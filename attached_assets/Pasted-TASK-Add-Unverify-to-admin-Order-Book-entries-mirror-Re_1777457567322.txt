TASK: Add "Unverify" to admin Order Book entries (mirror Revenue Entries flow)

CONTEXT
Revenue Entries already have a working admin unverify flow:
- Backend: POST /revenue-entries/:id/unverify in
  artifacts/api-server/src/routes/financials.ts
- Frontend: <UnverifyButton> in
  artifacts/brave-dashboard/src/pages/admin/project-detail.tsx

Order Book Entries do NOT have this. We need exact parity.

DO
1. Backend (financials.ts): add POST /order-book-entries/:id/unverify
   - Admin-only (req.user.role === "admin"; else 403)
   - Same row-lock + 409-on-already-submitted pattern as the revenue
     unverify route
   - Transitions status verified → submitted, clears
     verifiedAmount + adminNotes
   - Calls logAudit("unverify_order_book_entry", "order_book_entry",
     entry.id, …)
   - Sends a notification to the team leader (mirror revenue helper)

2. OpenAPI spec (lib/api-spec): add the new path + params + 200/403/404/409
   responses, copying the revenue unverify spec verbatim. Then run
   `pnpm --filter @workspace/api-spec run codegen` so
   `useUnverifyOrderBookEntry` and `UnverifyOrderBookEntryParams`
   are generated.

3. Frontend (admin/project-detail.tsx):
   - Add an "Actions" column to the Order book entries table, but ONLY
     when user.role === "admin" (match the revenue table conditional).
   - Render an Unverify button only when entry.status === "verified".
   - Create an OrderBookUnverifyButton component that mirrors the
     existing UnverifyButton: same AlertDialog wording, same toast on
     success/failure, useUnverifyOrderBookEntry hook.
   - On success, invalidate:
       getGetProjectQueryKey(projectId)
       getGetAdminReviewQueueQueryKey({ type: "order_book", status: "submitted" })
       getGetAdminReviewQueueQueryKey({ type: "order_book", status: "verified" })

DO NOT
- Touch coordinator/student views — admin-only.
- Change any unrelated revenue logic.
- Edit generated files by hand; regenerate via codegen.

VERIFY
- pnpm run typecheck passes.
- Visit /admin/projects/<id> with a verified order book entry → click
  Unverify → confirm → entry moves back to "submitted", appears in the
  admin review queue, leader receives a notification.
- Re-clicking returns 409 (already submitted).
- Coordinator role sees no Actions column.
